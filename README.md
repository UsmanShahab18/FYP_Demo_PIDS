# FYP_Demo_PIDS
A real-time Network Intrusion Detection System (IDS) that captures live traffic using Scapy and detects attacks using a 2-stage ML pipeline (XGBoost &amp; LightGBM). Features a Django backend and a ReactJS dashboard.
